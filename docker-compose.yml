services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: worklifebalance
      POSTGRES_USER: wlb
      POSTGRES_PASSWORD: wlb
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
